services:
  app:
    image: poxenstudio/neurapress:latest
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      # 粘贴图片的总容量上限（MB），默认 5120（见 README）
      # - IMAGE_STORAGE_LIMIT_MB=5120
    restart: unless-stopped
